# files generated by packer
packer_compiled.lua  

# installed plugins folders
# Git should onle version your configuration files (e.g., plugins.lua)
# and not the plugins themselves (which are large and tracked by the plugin manager).
/pack/
/plugged/
/lazy/

# caches. logs and temporary things
/cache/              # Pastas de cache de LSP, tree-sitter, etc.
/tmp/                # Arquivos temporários
/session/            # Sessões salvas pelo Neovim
*.log
*.luac               # Arquivos bytecode compilados de Lua

#commom files from the OS
.DS_Store            # macOS
Thumbs.db            # Windows
